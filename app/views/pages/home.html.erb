<div class="hero">
  <h1>Rekonect t’aide à répondre aux bons messages, au bon moment </h1>
  <p>Fini le stress digital ! On t’aide à retrouver ton rythme de réponse.</p>
</div>

<% if @awaiting_message %>
  <div class="alert alert-info position-relative">
    <%= button_to dismiss_suggestion_message_path(@awaiting_message), method: :patch, class: "btn btn-link btn-sm p-0 position-absolute", style: "top: 8px; right: 8px; color: #000;", title: "Ignorer cette suggestion" do %>
      <i class="bi bi-x-lg"></i>
    <% end %>
    <strong>Message en attente de réponse :</strong><br>
    <strong>De :</strong> <%= @awaiting_message.contact.name %><br>
    <strong>Message :</strong> <%= @awaiting_message.content %><br>
    <strong>Suggestion de réponse :</strong>
    <div style="background: #f8f9fa; padding: 10px; border-radius: 5px;">
      <%= @awaiting_message.ai_draft.presence || "Aucune suggestion disponible." %>
    </div>
    <%= link_to "Répondre", new_message_path(contact_id: @awaiting_message.contact_id), class: "btn btn-primary btn-sm" %>
  </div>
<% end %>
