<!DOCTYPE html>
<html>
  <head>
    <title>Rekonect</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
  </head>

  <body><%= render "shared/navbar" %>
<%= render "shared/flashes" %>

<% if controller_name == "pages" && action_name == "home" %>
  <nav>
    <ul>
      <li><%= link_to 'Accueil', root_path %></li>
      <li><%= link_to 'Mes messages', messages_path %></li>
      <li><%= link_to "Envoyer un message", new_message_path %></li>
      <li><%= link_to "Dashboard", dashboard_path %></li>
      <% if user_signed_in? %>
        <li><%= link_to 'Déconnexion', destroy_user_session_path, method: :delete %></li>
      <% else %>
        <li><%= link_to 'Connexion', new_user_session_path %></li>
      <% end %>
    </ul>
  </nav>
<% end %>

    <%= yield %>
  </body>
</html>
