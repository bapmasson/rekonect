<%= image_tag "logo.png", alt: "Logo Rekonect", class: "welcome-image"%>
<h1>Rekonect - Restes proche, simplement.</h1>
<p>Rekonect vous aide à reprendre contact avec vos proches en douceur. Des rappels personnalisés pour garder le lien, sans efforts.</p>
<h2 class="login-header">Log in</h2>

<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="form-inputs">
    <%= f.input :email,
                required: false,
                autofocus: true,
                input_html: { autocomplete: "email" } %>
    <%= f.input :password,
                required: false,
                input_html: { autocomplete: "current-password" } %>
    <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Log in" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
